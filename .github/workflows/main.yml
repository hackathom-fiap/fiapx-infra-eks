name: 'Deploy to Main'

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

concurrency:
  group: terraform-state-lock
  cancel-in-progress: false

jobs:
  deploy:
    name: 'Deploy Infrastructure'
    runs-on: ubuntu-latest
    
    defaults:
      run:
        shell: bash
        working-directory: ./terraform

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: us-east-1
          audience: sts.amazonaws.com

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Plan
        id: plan
        run: terraform plan -out=tfplan

      - name: Debug Secret Presence
        run: |
          if [ -z "${{ secrets.new_relic_api_key }}" ]; then
            echo "::error::Secret NEW_RELIC_API_KEY is NOT set or is empty."
            exit 1
          else
            echo "Secret NEW_RELIC_API_KEY is set."
          fi

      - name: Terraform Apply
        id: apply
        env:
          TF_VAR_newrelic_license_key: ${{ secrets.new_relic_api_key }}
        run: terraform apply -auto-approve tfplan